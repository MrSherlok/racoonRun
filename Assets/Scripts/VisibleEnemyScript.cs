using UnityEngine;
using System.Collections;

public class VisibleEnemyScript : MonoBehaviour {

/*	public GameObject enemy;
	public Vector3 pos;
	private float createCooldown = 0f;
	
	void FixedUpdate()
	{
		if (createCooldown > 0)
		{
			createCooldown -= Time.deltaTime;
		}
	
		if (GetComponent<Renderer>().IsVisibleFrom (Camera.main) && CanCreate) { 
			createCooldown = 1.5f;
			Instantiate(enemy,pos,Quaternion.identity); //,pos,Quaternion.identity
		}
	}


	public bool CanCreate {
		get {
			return createCooldown <= 0f;
		}
	}*/
}
